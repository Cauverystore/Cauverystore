{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend-java && mvn clean package -DskipTests"
  },
  "deploy": {
    "startCommand": "java -jar backend-java/target/*.jar",
    "healthcheckPath": "/"
  }
}
