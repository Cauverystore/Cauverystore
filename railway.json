{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend-java && mvn clean package -DskipTests"
  },
  "deploy": {
    "startCommand": "java -jar app.jar",
    "healthcheckPath": "/"
  }
}
